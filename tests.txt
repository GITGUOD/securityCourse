import time

def time_comparison(n):
    # Method 1: Using x = x^2 % n
    start_time = time.time()
    x = random.randint(2, n - 2)
    for _ in range(1000):  # Run a large number of iterations to measure time
        x = pow(x, 2, n)  # x = x^2 % n
    time_method_1 = time.time() - start_time

    # Method 2: Using x = a^(2^j) * s % n
    start_time = time.time()
    a = random.randint(2, n - 2)
    s = random.randint(1, n - 1)
    for _ in range(1000):  # Run a large number of iterations
        x = pow(a, 2, n) * s % n  # x = a^2 * s % n
    time_method_2 = time.time() - start_time

    print(f"Time for x = x^2 % n: {time_method_1:.6f} seconds")
    print(f"Time for x = a^2 * s % n: {time_method_2:.6f} seconds")

n = int(input("Enter a number to test the time difference: "))
time_comparison(n)
